var person={
    name :"hari" ,
    age :23 ,
    country :"india"
}
function logData(data) {
    console.log(` ${data.name} is ${data.age} years old and lives in ${data.country}`)
    
}
logData(person)
